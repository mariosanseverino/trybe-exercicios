let userValue = -66

if (userValue > 0) {
    console.log(`The value (${userValue}) is positive.`)
} else if (userValue < 0) {
    console.log(`The value (${userValue}) is negative.`)
} else {
    console.log(`Value inserted is zero.`)
}