const base = 5
const height = 8
const area = base * height
console.log(`The area of a base ${base} and height ${height} is ${area}.`)
const perimeter = (base * 2) + (height * 2)
console.log(`The perimeter of this rectangle is ${perimeter}.`)